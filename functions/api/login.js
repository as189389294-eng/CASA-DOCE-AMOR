async function hex(buf){return [...new Uint8Array(buf)].map(x=>x.toString(16).padStart(2,"0")).join("")}
export async function onRequestPost({request,env}){let {password}=await request.json();if(!env.ADMIN_PASSWORD||password!==env.ADMIN_PASSWORD)return new Response("Unauthorized",{status:401});let key=await crypto.subtle.importKey("raw",new TextEncoder().encode(env.SESSION_SECRET||env.ADMIN_PASSWORD),{name:"HMAC",hash:"SHA-256"},false,["sign"]);let sig=await crypto.subtle.sign("HMAC",key,new TextEncoder().encode("casa-doce-amor-admin"));return new Response(JSON.stringify({ok:true}),{headers:{"content-type":"application/json","Set-Cookie":`cda_admin=${await hex(sig)}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=604800`}})}
